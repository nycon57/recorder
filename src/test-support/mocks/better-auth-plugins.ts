export const admin = (config?: unknown) => ({ id: 'admin', config });
export const magicLink = (config?: unknown) => ({ id: 'magic-link', config });
export const organization = (config?: unknown) => ({
  id: 'organization',
  config,
});

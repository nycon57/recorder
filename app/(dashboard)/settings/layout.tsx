/**
 * Settings Layout
 * Settings pages with navigation in the main app sidebar
 */
export default function SettingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
